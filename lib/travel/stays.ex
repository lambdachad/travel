defmodule Travel.Stays do
  @moduledoc """
  Duffel Stays API namespace.

  Provides access to all Stays-related endpoints:

    * `Travel.Stays.Search` - Search for accommodations
    * `Travel.Stays.SearchResults` - Fetch all rates for a search result
    * `Travel.Stays.Quotes` - Create and retrieve quotes
    * `Travel.Stays.Bookings` - Create, retrieve, list, and cancel bookings
    * `Travel.Stays.Accommodation` - Get, list, suggest, and review accommodations
    * `Travel.Stays.Brands` - List and get hotel brands
    * `Travel.Stays.LoyaltyProgrammes` - List supported loyalty programmes
  """
end
